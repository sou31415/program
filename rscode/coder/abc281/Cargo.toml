[package]
name = "abc281"
version = "0.1.0"
edition = "2021"

# dependencies added to new project
[dependencies]
proconio={version = "0.4.3" , features = ["derive"]}
petgraph="0.6.3"
regex="1.7.1"
[profile.release]
lto = true
panic = 'abort'
