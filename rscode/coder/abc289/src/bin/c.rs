use proconio::{fastout, input};
use std::collections::HashSet;
fn main() {
    input! {
        n:usize , m:usize,
    }
    let mut set: HashSet<usize> = std::collections::HashSet::new();
    let mut dp = 0b0usize;
}
