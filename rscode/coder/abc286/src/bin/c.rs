#[allow(unused)]
use proconio::{input, marker::Chars};
use std::collections::VecDeque;
fn main() {
    input! {
        n:usize,
        a:usize,
        b:usize,
        s:String,
    }
    let mut result = s.len() / 2;
}
